<header class="container bg-light">
  <nav class="navbar navbar-expand navbar-light">
    <%= link_to "Devise", root_path, class: 'navbar-brand' %>
    <div id="Navber">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item active">
            <%= link_to User.human_attribute_name(:index), users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item active">
            <%= link_to User.human_attribute_name(:edit), edit_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item active">
            <%= link_to User.human_attribute_name(:destroy), destroy_user_session_path, method: :delete, class:
              'nav-link' %>
          </li>
          <li class="nav-item active">
            <%= link_to Book.human_attribute_name(:index), books_path, class: 'nav-link' %>
          </li>
      <% else %>
          <li class="nav-item active">
            <%= link_to User.human_attribute_name(:new), new_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item active">
            <%= link_to User.human_attribute_name(:create), new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>
